<Application x:Class="GB_Payroll_System.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GB_Payroll_System"
             StartupUri="Views/LoginWindow.xaml">
    <Application.Resources>
        <!-- Color Palette matching Genetian Design -->
        <Color x:Key="BgGradientStartColor">#0A4D9C</Color>
        <Color x:Key="BgGradientEndColor">#06254A</Color>
        <Color x:Key="PrimaryVividBlueColor">#002CFA</Color>
        <Color x:Key="PrimaryVividBlueHoverColor">#0022C8</Color>
        <Color x:Key="CardBgColor">#FFFFFF</Color>
        <Color x:Key="InputBgColor">#F5F6F8</Color>
        <Color x:Key="InputBorderColor">#E2E8F0</Color>
        <Color x:Key="TextDarkColor">#1A202C</Color>
        <Color x:Key="TextMutedColor">#718096</Color>
        <Color x:Key="ConnectedGreenColor">#00C853</Color>

        <SolidColorBrush x:Key="PrimaryVividBlueBrush" Color="{StaticResource PrimaryVividBlueColor}"/>
        <SolidColorBrush x:Key="PrimaryVividBlueHoverBrush" Color="{StaticResource PrimaryVividBlueHoverColor}"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="{StaticResource CardBgColor}"/>
        <SolidColorBrush x:Key="InputBgBrush" Color="{StaticResource InputBgColor}"/>
        <SolidColorBrush x:Key="InputBorderBrush" Color="{StaticResource InputBorderColor}"/>
        <SolidColorBrush x:Key="TextDarkBrush" Color="{StaticResource TextDarkColor}"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
        <SolidColorBrush x:Key="ConnectedGreenBrush" Color="{StaticResource ConnectedGreenColor}"/>

        <!-- Radial Background Gradient -->
        <RadialGradientBrush x:Key="LoginWindowBgBrush" Center="0.5,0.3" GradientOrigin="0.5,0.3" RadiusX="0.8" RadiusY="0.8">
            <GradientStop Color="{StaticResource BgGradientStartColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource BgGradientEndColor}" Offset="1.0"/>
        </RadialGradientBrush>

        <!-- Primary Action Button Style -->
        <Style x:Key="PrimaryBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryVividBlueBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="46"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryVividBlueHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Input Field Style -->
        <Style x:Key="ModernInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource TextDarkBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryVividBlueBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
